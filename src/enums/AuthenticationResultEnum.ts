/**
 * Enum for Authentication result.
 * @readonly
 * @enum {string}
 */
export enum AuthenticationResultEnum {
  Success = 'Success',
  NotFound = 'NotFound',
  Inactive = 'Inactive',
  Invalid = 'Invalid',
}