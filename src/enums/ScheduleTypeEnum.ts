/**
 * Enum for schedule types.
 * @enum {string}
 */
const ScheduleTypeEnum = Object.freeze({
  Scheduled: 'Scheduled', // Scheduled type
  Interactive: 'Interactive', // Interactive type
});

// Export the enum for use in other modules
module.exports = ScheduleTypeEnum;
